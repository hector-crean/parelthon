import { VideoAnnotation } from "@/models/annotation"
import { Point } from "geojson"


interface PointAnnotationProps {
    annotation: VideoAnnotation<Point>
}
const PointAnnotation = (props: VideoAnnotation<Point>) => {


    return (<div></div>)
}